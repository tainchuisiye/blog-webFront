import request, { errorProcess } from 'src/utils/request';
export default {
  /*
   * 获取资金方列表
   * @param {*}
   */
  // async getCapitalLists() {
  //   return request({
  //     method: 'get',
  //     url: `/eam/api/capitals/queryAll`
  //   }).then(errorProcess);
  // },
};